% Given a positive integer num, output its complement number. The
% complement strategy is to flip the bits of its binary representation.

num = 5;
output = findComplement(num)

num = 1;
output = findComplement(num)