% Given a non-empty array nums containing only positive integers, find if
% the array can be partitioned into two subset such that the sum of
% elements in both subsets is equal.

nums = [1, 5, 11, 5];
output = canPartition(nums)

nums = [1, 2, 3, 5];
output = canPartition(nums)