% A sequence of numbers is called arithmetic if it consists of at least
% three elements and if the difference between any consecutive element is
% the same.

A = [1, 2, 3, 4];
output = numberOfArithmeticSlices(A)